stages:
  - configure
  - build
  - test

configure:
  stage: configure
  before_script:
      - chmod +x gitlab-ci/*
      - git submodule update --init --recursive
  script: "gitlab-ci/configure.sh"

buildlibs:
  stage: build
  script: "gitlab-ci/build.sh"